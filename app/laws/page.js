import AskTheRef from "../../components/laws/AskTheRef";

export const metadata = {
  title: "Ask the Ref — FIFA Law RAG Explainer | DECODED",
  description: "Type any football rule question and get the official FIFA Law article translated into plain English using Docling semantic search and IBM Granite AI.",
};

export default function LawsPage() {
  return <AskTheRef />;
}
